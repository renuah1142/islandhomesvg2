"use client";
import { useEffect, useRef, useState } from "react"; import gsap from "gsap";
const links = [["Homes", "#models"], ["Why us", "#why"], ["Process", "#process"], ["Gallery", "#gallery"], ["Contact", "#contact"]];
export default function Nav() { const [open, setOpen] = useState(false); const nav = useRef<HTMLElement>(null); useEffect(() => { const on = () => { const scrolled = window.scrollY > 48; if (nav.current) { gsap.to(nav.current, { backgroundColor: scrolled ? "rgba(255,255,255,.78)" : "rgba(255,255,255,0)", color: scrolled ? "#1b1b1b" : "#ffffff", borderColor: scrolled ? "rgba(27,27,27,.12)" : "rgba(255,255,255,.22)", boxShadow: scrolled ? "0 16px 45px rgba(0,0,0,.08)" : "0 0 0 rgba(0,0,0,0)", duration: .45, ease: "power2.out" }) } }; on(); window.addEventListener("scroll", on, { passive: true }); return () => window.removeEventListener("scroll", on) }, []); return <header className="nav-shell fixed inset-x-0 top-0 z-50 px-3 pt-3 md:px-5 md:pt-5"><nav ref={nav} className="nav-panel mx-auto flex max-w-[1280px] items-center justify-between border border-white/20 px-5 py-3 text-white shadow-none md:px-7"><a href="#" className="leading-none"><span className="font-bold tracking-[.18em]">ISLAND <span className="text-[#b8a890]">HOMES</span></span><small className="mt-1 block text-center text-[8px] tracking-[.42em] opacity-65">SVG</small></a><div className="hidden items-center gap-8 text-[11px] font-semibold uppercase tracking-[.16em] md:flex">{links.map(([label, href]) => <a key={href} href={href} className="group relative py-3 opacity-75 transition hover:opacity-100"><span>{label}</span><span className="absolute bottom-0 left-0 h-px w-0 bg-current transition-all duration-300 group-hover:w-full" /></a>)}</div><a href="#contact" className="hidden border border-current px-5 py-3 text-[11px] font-bold uppercase tracking-[.12em] md:block">Get a quote</a><button onClick={() => setOpen(!open)} className="md:hidden" aria-label="Toggle menu" aria-expanded={open}><span className="text-xl">{open ? "×" : "☰"}</span></button>{open && <div className="absolute left-3 right-3 top-[calc(100%+.5rem)] border border-black/10 bg-white p-5 text-[#1b1b1b] shadow-2xl md:hidden">{links.map(([label, href]) => <a key={href} onClick={() => setOpen(false)} href={href} className="block border-b border-black/10 py-4 text-xs font-bold uppercase tracking-[.16em] last:border-0">{label}</a>)}<a onClick={() => setOpen(false)} href="#contact" className="mt-3 block bg-[#161616] px-5 py-3 text-center text-xs font-bold uppercase tracking-[.12em] text-white">Get a quote</a></div>}</nav></header> }
