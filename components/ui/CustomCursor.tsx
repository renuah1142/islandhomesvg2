"use client";
import {useEffect} from "react";import gsap from "gsap";
export default function CustomCursor(){useEffect(()=>{if(window.matchMedia("(pointer:coarse)").matches||window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;const dot=document.querySelector<HTMLElement>(".cursor-dot"),ring=document.querySelector<HTMLElement>(".cursor-ring");if(!dot||!ring)return;const move=(e:MouseEvent)=>{gsap.to(dot,{x:e.clientX,y:e.clientY,duration:.08});gsap.to(ring,{x:e.clientX,y:e.clientY,duration:.35,ease:"power3.out"})};window.addEventListener("mousemove",move);const enter=()=>gsap.to(ring,{scale:1.55,duration:.25}),leave=()=>gsap.to(ring,{scale:1,duration:.25});document.querySelectorAll("a,button").forEach(el=>{el.addEventListener("mouseenter",enter);el.addEventListener("mouseleave",leave)});return()=>window.removeEventListener("mousemove",move)},[]);return <><div className="cursor-dot"/><div className="cursor-ring"/></>}
