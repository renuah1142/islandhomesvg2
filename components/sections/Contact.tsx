"use client";
import { useState } from "react";
import { useForm } from "react-hook-form";
import { z } from "zod";
import { zodResolver } from "@hookform/resolvers/zod";
const schema = z.object({ name: z.string().min(2, "Please enter your name"), email: z.string().email("Enter a valid email"), phone: z.string().optional(), interest: z.string().min(1), message: z.string().min(10, "Please tell us a little more"), website: z.string().max(0).optional() });
type FormData = z.infer<typeof schema>;
export default function Contact() {
    const [sent, setSent] = useState(false), [error, setError] = useState("");
    const { register, handleSubmit, reset, formState: { errors, isSubmitting } } = useForm<FormData>({ resolver: zodResolver(schema), defaultValues: { interest: "General inquiry", website: "" } });
    const submit = async (data: FormData) => { setError(""); try { const res = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); const body = await res.json().catch(() => ({})); if (!res.ok) throw new Error(body.error || "Unable to send"); setSent(true); reset() } catch (e) { setError(e instanceof Error ? e.message : "We couldn't send the form right now. Please WhatsApp or call +1 (784) 497-6196.") } };
    return <section id="contact" className="section bg-[#161616] text-white"><div className="container grid gap-10 md:grid-cols-[.9fr_1.1fr] md:gap-12 md:items-start">
        <div className="reveal-item"><p className="eyebrow">Let's build your dream home</p><h2 className="mt-3 text-balance text-5xl font-black tracking-[-.05em] md:text-7xl">Get your free quote.</h2><p className="mt-6 max-w-lg text-lg leading-relaxed text-white/65">Ready to get started? Call, WhatsApp or email Island Homes SVG to discuss your needs and receive a personalized quotation.</p><div className="mt-9 space-y-4 text-white/85"><a href="https://wa.me/17844976196" target="_blank" rel="noreferrer" className="block hover:text-[#c99227]"><span className="block text-xs uppercase tracking-widest text-[#c99227]">WhatsApp</span><strong className="text-xl">+1 (784) 497-6196 ↗</strong></a><a href="tel:+17844976196" className="block hover:text-[#c99227]"><span className="block text-xs uppercase tracking-widest text-[#c99227]">Phone</span><strong className="text-xl">+1 (784) 497-6196</strong></a><a href="mailto:islandhomes.svg@gmail.com" className="block hover:text-[#c99227]"><span className="block text-xs uppercase tracking-widest text-[#c99227]">Email</span><strong className="text-xl">islandhomes.svg@gmail.com</strong></a><p><span className="block text-xs uppercase tracking-widest text-[#c99227]">Location</span><strong>St. Vincent & the Grenadines</strong></p></div></div>
        <div className="reveal-item rounded-[2rem] bg-white p-6 text-[#10233b] md:p-9">{sent ? <div className="flex min-h-[430px] flex-col items-center justify-center text-center"><div className="grid h-20 w-20 place-items-center rounded-full bg-[#f7f4ed] text-5xl text-[#c99227]">✓</div><h3 className="mt-6 text-3xl font-black">Thanks for reaching out.</h3><p className="mt-3 max-w-md text-slate-600">Your inquiry has been sent. For immediate assistance, WhatsApp +1 (784) 497-6196.</p><button onClick={() => setSent(false)} className="mt-7 rounded-full bg-[#061b33] px-6 py-3 font-bold text-white">Send another</button></div> : <form onSubmit={handleSubmit(submit)} className="space-y-4"><div className="grid gap-4 md:grid-cols-2"><label className="block text-sm font-bold">Name<input {...register("name")} autoComplete="name" className="mt-2 w-full rounded-xl border border-slate-200 p-4 font-normal outline-none focus:border-[#c99227]" placeholder="Your name" />{errors.name && <span className="mt-1 block text-xs text-red-600">{errors.name.message}</span>}</label><label className="block text-sm font-bold">Email<input {...register("email")} type="email" autoComplete="email" className="mt-2 w-full rounded-xl border border-slate-200 p-4 font-normal outline-none focus:border-[#c99227]" placeholder="you@example.com" />{errors.email && <span className="mt-1 block text-xs text-red-600">{errors.email.message}</span>}</label></div><label className="block text-sm font-bold">Phone / WhatsApp <span className="font-normal text-slate-400">(optional)</span><input {...register("phone")} autoComplete="tel" className="mt-2 w-full rounded-xl border border-slate-200 p-4 font-normal outline-none focus:border-[#c99227]" placeholder="+1 (784) ..." /></label><label className="block text-sm font-bold">I'm interested in<select {...register("interest")} className="mt-2 w-full rounded-xl border border-slate-200 bg-white p-4 font-normal outline-none focus:border-[#c99227]"><option>General inquiry</option><option>20FT Oasis</option><option>30FT Family</option><option>40FT Signature</option><option>Pinnacle Two Story</option><option>Buying / custom project</option></select></label><label className="block text-sm font-bold">Message<textarea {...register("message")} rows={5} className="mt-2 w-full rounded-xl border border-slate-200 p-4 font-normal outline-none focus:border-[#c99227]" placeholder="Tell us what you need..." />{errors.message && <span className="mt-1 block text-xs text-red-600">{errors.message.message}</span>}</label><input {...register("website")} tabIndex={-1} autoComplete="off" aria-hidden="true" className="absolute -left-[9999px] h-0 w-0 opacity-0" />{error && <p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={isSubmitting} className="w-full rounded-full bg-[#c99227] px-6 py-4 font-black text-[#061b33] transition hover:scale-[1.01] disabled:opacity-60">{isSubmitting ? "Sending..." : "Request My Free Quote ↗"}</button><p className="text-center text-xs text-slate-400">Your details are used only to respond to this inquiry.</p></form>}</div>
    </div></section>
}
